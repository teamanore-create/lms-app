# Database Connection
# Format: postgresql://[user[:password]@][netloc][:port][/dbname][?param1=value1&...]
DATABASE_URL="postgresql://lms_user:StrongPassword123@localhost:5432/lms_dbs"

# Better Auth Secret
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET="your-secret-key-here-min-32-chars"

# Better Auth URL (optional)
# Will auto-detect from VERCEL_URL if not set
# BETTER_AUTH_URL="http://localhost:3000"


SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="rembico.connect@gmail.com"
SMTP_PASS="cunr naax oysy vnrc"
SMTP_FROM="BIM Training <noreply@yourdomain.com>"
# Node Environment
NODE_ENV="development"